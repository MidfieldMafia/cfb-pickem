# Domain Docs

Single-context repo. Read `CONTEXT.md` at the repo root before exploring; use its terms in issue titles, code names, and tests. Architecture decisions live in `docs/adr/`. Research findings live in `docs/research/`. If a file is missing, proceed silently.
